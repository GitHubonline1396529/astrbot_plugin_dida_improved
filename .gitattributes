# Only secrets and the attributes file itself are excluded from dev ZIP.
# All other files (docs, tests, configs) are intentionally included
# so that collaborators get a complete development environment.
.gitattributes    export-ignore
.env              export-ignore
